package dev.megaport.sorter.impl;

import dev.megaport.sorter.Sorter;
import dev.megaport.sorter.data.TwoParam;

import java.util.Comparator;

/**
 * Two param sorter
 */
public class TwoParamSorter extends Sorter<TwoParam> {

    @Override
    public void sort(Comparator<TwoParam> comparator) {
        super.sort(comparator);
    }
}
